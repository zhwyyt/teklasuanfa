# GitHub 同步策略

## 文档目的

本文档用于固定本仓库后续的 GitHub 同步规则，避免把历史实验分支、半成品试验和局部临时验证持续堆到远端。

目标是让 GitHub 上只保留：

1. 当前明确的活跃工作主线
2. 少量值得长期回看和回退的冻结里程碑

---

## 总原则

GitHub 不再作为“所有尝试都往上堆”的地方，而只同步以下两类分支：

1. 当前唯一活跃 `work` 分支
2. 少量关键 `freeze` 分支

历史混乱实验分支不再继续向 GitHub 更新。

---

## 分支分类

### 1. 稳定主干

用途：

- 放确认稳定、适合作为长期基线的版本

要求：

- 不在其上直接做实验
- 只接收已经验证收敛的结果

### 2. 工作分支

命名：

- `codex/work-*`

用途：

- 当前唯一活跃开发主线

规则：

1. 同一时间原则上只保留一条活跃 `work` 分支
2. 一条 `work` 分支只推进一个明确主题
3. 允许阶段性推送到 GitHub

当前活跃工作分支：

- `codex/work-variable-section-proof-20260428`

### 3. 冻结分支

命名：

- `codex/freeze-*`

用途：

- 冻结关键里程碑，供回退、对照、长期回看

适用场景：

1. 上游真实中线重建定版
2. `PolyBeam + Beam` guide 候选定版
3. 控制面 V2 切换完成
4. 某一类 proof / 家族映射规则达到稳定收敛

规则：

- 只在关键节点推送
- 不作为日常活跃开发分支

### 4. 历史实验分支

用途：

- 仅作本地/历史参考

规则：

1. 不再继续开发
2. 原则上不再补推新提交到 GitHub
3. 只有在必须回看历史原因时才引用

---

## 推送节奏

### 小改动

- 先留在本地
- 不要求立刻推 GitHub

### 一个主题完成一个可验证小阶段

- 推送当前 `work` 分支一次

### 一个明确稳定里程碑形成

- 除推 `work` 分支外
- 额外切一个 `freeze` 分支并推送

---

## 推送前检查

每次推送 GitHub 前，至少满足以下条件：

1. 当前变更范围清楚
2. 能一句话说明“这次推送解决了什么”
3. 状态板/任务板已同步到当前执行口径
4. 如涉及代码变更，至少完成最小构建或最小验证
5. 不把无关临时目录、历史噪音、半截试验一并混入

---

## 提交与推送边界

### 允许推送的内容

1. 已形成清晰阶段结论的代码变更
2. 与当前主题直接相关的状态文档、任务文档、策略文档
3. 能支撑回归和复核的必要输出契约文档

### 不应推送的内容

1. 临时试验脚本
2. 未收敛的样本特判补丁
3. 无意义的缓存、临时目录、验证中间产物
4. 与当前主题无关的大量混杂改动

---

## 当前执行策略

从本文档生效起，当前仓库采用以下 GitHub 同步策略：

1. 活跃开发只沿：
   - `codex/work-variable-section-proof-20260428`
2. 后续需要长期冻结的节点，再单独切：
   - `codex/freeze-*`
3. 历史实验分支只保留参考价值，不再继续向 GitHub 同步新内容

---

## 与控制面的关系

本策略文档与以下文档配套使用：

1. [PROJECT_STATUS_V2.zh-CN.md](</I:/autoteklasuanfa/PROJECT_STATUS_V2.zh-CN.md>)
2. [PROJECT_TASKLIST_V2.zh-CN.md](</I:/autoteklasuanfa/PROJECT_TASKLIST_V2.zh-CN.md>)
3. [PROJECT_REFOCUS_PLAN.zh-CN.md](</I:/autoteklasuanfa/PROJECT_REFOCUS_PLAN.zh-CN.md>)

其中：

- `PROJECT_STATUS_V2` 负责当前状态
- `PROJECT_TASKLIST_V2` 负责当前任务
- `PROJECT_REFOCUS_PLAN` 负责高层整编逻辑
- 本文档负责远端同步纪律
