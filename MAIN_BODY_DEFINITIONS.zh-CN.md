# 主材识别工程定义

## 文档目的

本文档用于冻结“主材识别”相关的核心工程定义，作为后续主体识别基座重构的判定规范。

使用原则：

- 先满足定义，后输出结论。
- 定义不满足时，允许输出 `UNKNOWN`、`IRREGULAR` 或 `REVIEW_REQUIRED`。
- 不允许仅凭启发式投票结果直接替代工程定义。

---

## 1. 判定对象

主材识别的直接判定对象不是：

- 单块板
- 板链数量
- 上游预分类标签
- 单一视角下的几何印象

主材识别的直接判定对象应当是：

- 构件主轴
- 主轴稳定区
- 稳定区上的横截面主轮廓
- 构成该主轮廓的真实零件集合

---

## 2. 真实零件与虚拟对象

### 2.1 真实零件

真实零件是指 Tekla 模型中实际存在的 `part_id` 对应对象。

真实零件是后续所有主体结论的最终依据。

### 2.2 虚拟对象

虚拟对象仅允许作为中间解释对象存在，例如：

- 虚拟长向板链
- 合成主体壁板
- 站位迹线分组

约束：

- 虚拟对象必须保留 `source_part_ids`
- 最终输出不得只给虚拟件编号，不给真实零件编号

---

## 3. 主材

主材定义如下：

主材是沿构件主轴方向形成主要承载截面，并在多数稳定站位上参与主截面解释的真实零件集合。

必须同时满足：

1. 该零件沿构件主轴具有显著持续性；
2. 该零件在多数稳定站位上参与主截面；
3. 去除该零件后，主截面家族、主截面拓扑或关键主尺寸发生显著变化；
4. 该零件不是仅服务于局部连接、局部稳定或局部补强的构造件。

不属于主材的典型对象：

- 耳板
- 端板
- 马板
- 覆板
- 衬板
- 局部补强板
- 局部加劲板
- 牛腿板件

---

## 4. 主体核心零件

主体核心零件是主材中的最小必要集合。

判定标准：

1. 在多数稳定站位上，该零件参与主截面主轮廓；
2. 去除该零件后，主体家族改变，或主轮廓关键拓扑改变；
3. 去除该零件后，闭合关系、对称关系或主尺寸显著变化；
4. 该零件的作用不是局部性，而是截面级别的。

主体核心零件数量没有先验固定值。

因此：

- 不能先假设 `H = 3 块`
- 不能先假设 `BOX = 4 块`
- 应先通过主截面证明，再反推出核心零件数量

---

## 5. 主体附属零件

主体附属零件定义如下：

沿主体长向共存、可能持续较长，但去除后主体主截面家族与主轮廓不变，只影响局部刚度、局部稳定或局部构造的零件。

典型对象：

- 纵向加劲板
- 覆板
- 衬板
- 局部补强板
- 局部隔板

判定原则：

- “持续性高”不等于“属于主体核心”
- 只有“去除后改变主截面证明”的零件才能升级为核心

---

## 6. 稳定区

稳定区是允许参与主体定义的长度区间。

稳定区必须满足：

1. 远离两端连接复杂区；
2. 不被耳板、端板、连接件密集区主导；
3. 截面主拓扑在相邻站位间基本一致；
4. 不依赖局部构造件才能解释主轮廓。

禁止：

- 用两端复杂连接区直接定义主体家族
- 用局部特殊区代表整体主体

---

## 7. 稳定站位

稳定站位是位于稳定区内、可用于截面判定的横截面抽样平面。

一个站位可用作主体判定证据，必须满足：

1. 主轮廓可稳定重建；
2. 主轮廓与真实零件映射清晰；
3. 局部噪声线、极短线段、临时构造线已清理；
4. 截面解释不依赖单个局部小件。

---

## 8. 焊接 H / I 的定义

某构件可判为焊接 H / I，当且仅当在多数稳定站位上同时满足：

1. 存在一条主腹板；
2. 存在两条主翼缘；
3. 两条主翼缘位于主腹板两侧；
4. 主腹板与主翼缘近似正交；
5. 主腹板与两主翼缘沿长度方向稳定持续；
6. 去除局部加劲板、衬板、耳板后，上述关系仍成立。

不满足以下任一条件时，不得判为 H：

- 两翼缘并不构成稳定对称关系；
- 主截面需要依赖多个局部小板才能解释；
- 所谓“腹板/翼缘”只是局部短板或次级构造件；
- 多数站位上该拓扑并不稳定。

---

## 9. 焊接箱型的定义

某构件可判为焊接箱型，当且仅当在多数稳定站位上同时满足：

1. 主截面形成闭合或准闭合主轮廓；
2. 主轮廓可归约为四壁闭合关系；
3. 对边关系稳定；
4. 去除内部加劲板、局部隔板后，闭合关系仍成立；
5. 闭合关系不是由耳板、端板、马板、衬板偶然补齐。

不得判为 BOX 的情形：

- 闭合只在端部局部区成立；
- 闭合依赖局部连接件；
- 多数站位上并不存在稳定四壁关系；
- 所谓“箱”只是多块局部板围成的短区段局部围合。

---

## 10. 焊接 T 的定义

某构件可判为焊接 T，当且仅当在多数稳定站位上同时满足：

1. 存在一条主腹板；
2. 存在一条主翼缘；
3. 主翼缘只位于腹板一侧；
4. 该关系在长度方向稳定；
5. 去除局部补强后，该关系不变。

---

## 11. 十字 / 组合型的定义

某构件可判为十字 / 组合型，当且仅当在多数稳定站位上同时满足：

1. 存在两条或以上主腹板；
2. 主腹板之间形成稳定正交或规则组合关系；
3. 主轮廓不能被更简单地解释为 H、BOX 或 T；
4. 该关系在长度方向稳定。

---

## 12. 异形与未知

### 12.1 IRREGULAR

满足以下任一条件时，应判为 `IRREGULAR`：

1. 主截面拓扑在稳定区内变化明显；
2. 无法稳定匹配 H、BOX、T、CROSS；
3. 主体解释依赖多个局部板件拼凑；
4. 曲板、折板、特殊对象深度参与主体形成。

### 12.2 UNKNOWN

满足以下任一条件时，应判为 `UNKNOWN`：

1. 主轴无法稳定确定；
2. 稳定区无法可靠建立；
3. 截面证据不足；
4. 主体候选和局部构造件难以分层。

---

## 13. REVIEW_REQUIRED 的触发条件

以下情况必须进入复核：

1. 两个家族定义同时接近满足；
2. 核心零件集合不稳定；
3. 主体结论依赖上游缓存预分类；
4. 真实零件与虚拟主体结论之间存在明显偏差；
5. 典型样本与工程直觉冲突；
6. 去除重算结论前后不一致；
7. 主体核心数量异常；
8. 站位间主截面证明冲突。

---

## 14. 主材判定的禁止事项

后续实现中，以下做法视为不合规：

1. 仅凭板链数量决定 H / BOX；
2. 仅凭上游 `MainClass` 直接输出主体家族；
3. 仅凭单张视角或单个局部截图做家族结论；
4. 先假定核心件数量，再倒推截面家族；
5. 输出结论时不给真实零件编号；
6. 用局部端部复杂区替代稳定区。

---

## 15. 当前样本口径要求

在新基座完成前，当前系统的主体输出必须明确区分三类信息：

1. `ImportSynthesisKind`
2. `BodyDescriptorFamily`
3. `RealSourceBodySeedParts`

且必须避免把启发式家族直接表述为“最终工程结论”。

---

## 16. 对 T3-1HXZ-1 一类边界样本的解释要求

对这类样本，后续系统必须能直接回答：

1. 哪些真实零件被视为主体种子零件；
2. 它们在多数稳定站位上是否真的构成：
   - 一腹板两翼缘
   - 四壁闭合
   - 一腹板单翼缘
3. 去除任一关键零件后是否改变主截面家族；
4. 若不能证明，则为什么进入 `IRREGULAR / UNKNOWN / REVIEW_REQUIRED`。

---

## 17. 本文档的强约束地位

后续主材识别代码重构时：

- 若实现与本文档冲突，以本文档定义为准；
- 若样本显示定义本身不足，应先修本文档，再改代码；
- 不允许在未更新定义文档的情况下，私自引入新的家族判定口径。
