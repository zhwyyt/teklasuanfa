# Tekla 主材识别基座重构方案

## 文档定位

本文档不是代码设计细节补丁，而是对“主材识别”问题本身重新定义基座。

这份文档承担“基线方案 / 定义依据”的职责，不承担逐轮进度同步：

- 最新阶段状态、已验证样本与当前下一步，看 [MAIN_BODY_REBUILD_STATUS.md](</I:/autoteklasuanfa/MAIN_BODY_REBUILD_STATUS.md>)
- 当前执行清单与阶段落地顺序，看 [MAIN_BODY_REBUILD_TASKLIST.zh-CN.md](</I:/autoteklasuanfa/MAIN_BODY_REBUILD_TASKLIST.zh-CN.md>)

目标不是继续微调当前 `BodyRecognizer` 的投票规则，而是先回答四个根问题：

1. 什么叫主材。
2. 什么叫主体核心零件。
3. 什么叫焊接 H、箱型、T、十字、异形。
4. 算法应该先判什么、后判什么，哪些结论必须由工程定义支撑。

本文档的直接用途：

- 作为后续重构主体识别模块的基线方案。
- 作为测试与验收的定义依据。
- 作为解释当前误判的统一口径。

---

## 一、当前基座存在的核心问题

### 1. 当前系统是启发式驱动，不是工程定义驱动

当前主体识别大体采用：

- 候选板件筛选
- 主轴估计
- 虚拟板链构建
- 板链法向投票
- 选取前 `3/4` 条强链作为主体核心

问题在于：

- “H / BOX / T” 的判定条件不是先验定义，而是投票赢家。
- “主体核心”不是由去除后是否改变主截面来定义，而是由链持续性排序近似得到。
- “主材零件”与“合成虚拟板链”混用，容易失去真实零件语义。

### 2. 当前判定对象错位

当前系统主要对“板链集合”做判定。  
但工程上真正应判定的对象是：

- 构件主轴
- 主轴中部稳定区
- 稳定区上的横截面主轮廓
- 构成主轮廓的真实零件集合

也就是说，正确问题应是：

`给定一组真实零件，哪些零件在多数稳定截面站位上构成主受力截面？`

而不是：

`这组板链看起来更像 H 还是 BOX？`

### 3. 缺少“真实零件 -> 算法结论”的可追溯定义

当前输出虽然可以列出核心件，但对合成主体会出现两层对象：

- 真实零件 `part_id`
- 算法生成的虚拟零件 `-1001/-1002/...`

若没有显式映射，工程人员无法知道：

- 哪些真实板被当成主材候选。
- 哪些真实板被剔除为附属件。
- 为什么某块板没有进入主体核心。

### 4. 当前“家族判定”早于“主截面证明”

工程上应先证明：

- 主体稳定截面是什么。
- 主体核心零件有哪些。

之后才能判：

- 该主截面属于 H / BOX / T / CROSS / IRREGULAR 的哪一类。

当前系统则存在“先投票得出家族，再倒推核心件数量”的倾向，这会导致定义反向依赖。

---

## 二、重构后的目标

重构后的主体识别基座，应满足以下目标：

1. **定义先行**：每个主体家族都必须有明确工程定义。
2. **真实零件优先**：所有关键输出必须可回溯到真实零件编号。
3. **截面优先**：先重建稳定截面，再做家族判断。
4. **可剔除验证**：主体核心零件必须能通过“去除后主截面改变”来验证。
5. **解释可审计**：每次判定都能输出证据，不靠黑盒分值。
6. **允许未知**：当不满足定义时，宁可输出“未知/异形/需复核”，不能强判。

---

## 三、术语与严格工程定义

## 3.1 主材

主材是指：

- 沿构件主轴方向形成主要承载体系；
- 在多数稳定横截面站位上参与主截面解释；
- 去除后会导致主截面家族变化、主截面尺寸显著变化，或主截面闭合关系消失的零件集合。

主材不等于：

- 所有长条板；
- 所有大板；
- 所有被焊在主体上的板；
- 所有参与外包络的板。

## 3.2 主体核心零件

主体核心零件是主材中的最小必要集合，满足：

1. 在多数稳定站位上参与主截面；
2. 去掉任一关键核心零件后，主截面家族或关键拓扑改变；
3. 不依赖局部端部构造才能成立；
4. 不依赖局部加劲板、衬板、耳板、牛腿才能成立。

## 3.3 主体附属零件

主体附属零件是：

- 与主体沿长向共存，
- 但去掉后主截面家族不变，
- 仅影响局部刚度、局部稳定、局部连接或制造构造的零件。

典型对象：

- 纵向加劲板
- 覆板 / 衬板
- 局部补强板
- 耳板
- 端板

## 3.4 稳定截面站位

稳定截面站位是指：

- 远离两端局部连接复杂区；
- 横截面主拓扑在相邻站位间基本一致；
- 不受端板、连接件、局部耳板主导影响的站位。

只有稳定截面站位才允许参与主体家族定义。

## 3.5 焊接 H 的严格定义

某构件可判为焊接 H，当且仅当在多数稳定站位上同时满足：

1. 存在且仅存在一条主腹板迹线；
2. 存在两条主翼缘迹线；
3. 两翼缘分居腹板两侧；
4. 腹板与翼缘近似正交；
5. 两翼缘在主尺度上持续存在；
6. 去除局部加劲板后，上述关系仍成立。

若不满足以上条件，不能判为 H。

## 3.6 焊接箱型的严格定义

某构件可判为焊接箱型，当且仅当在多数稳定站位上同时满足：

1. 主截面存在闭合或准闭合主轮廓；
2. 该主轮廓可归约为四壁闭合关系；
3. 对边关系稳定；
4. 去除内部加劲件后闭合关系仍存在；
5. 闭合并非由局部耳板、衬板、端板、隔板偶然补齐。

若闭合关系依赖局部件，不可判为 BOX。

## 3.7 焊接 T 的严格定义

某构件可判为焊接 T，当且仅当在多数稳定站位上同时满足：

1. 存在一条主腹板；
2. 存在一条主翼缘；
3. 翼缘只位于腹板一侧；
4. 去除局部补强后拓扑稳定。

## 3.8 十字 / 组合型的严格定义

若多数稳定站位上存在两条相互正交且均为主受力腹板的主迹线，且构成稳定组合截面，可判为十字 / 组合型。

## 3.9 异形 / 未定义

若满足以下任一条件，应输出 `IRREGULAR` 或 `UNKNOWN`，而非强判为 H / BOX / T：

- 主截面拓扑在长度方向不稳定；
- 同时接近多种家族且无法证明；
- 主要截面依赖大量局部板；
- 无法形成最小必要主材集合；
- 特殊板件深度参与主体。

---

## 四、重构后的判定分层

主体识别必须改为分层判定，不允许跳步。

## L0：数据标准化层

输入只保留真实零件语义：

- `assembly_id`
- `member_id`
- `main_part_id`
- `part_id`
- `name`
- `profile`
- `material`
- `runtime_type`
- `thickness`
- `solid / bbox / centroid / local_cs`
- `weld / boolean / bolt / contact`

要求：

- 任何后续虚拟对象都必须保留 `source_part_ids`
- 不允许只输出虚拟件编号而没有真实来源

## L1：候选零件分层

先把零件分成：

- 主体候选
- 局部连接候选
- 端部构造候选
- 小件 / 噪声
- 特殊形状件

这一层只做“进入哪个池”，不做家族判定。

## L2：主轴与稳定区确定

目标：

- 确定构件主轴
- 确定应参与判定的稳定区间

稳定区必须排除：

- 两端连接复杂区
- 局部耳板高密区
- 明显端板主导区

## L3：横截面重建层

这是新基座的核心层。

对于每个稳定站位：

1. 让真实零件与截面平面求交；
2. 得到每个零件的截面迹线；
3. 清洗重合线、短噪声线；
4. 标记外包络线与内部线；
5. 记录“哪条线来自哪个真实零件”。

最终输出：

- 站位主轮廓
- 站位主截面候选线段
- 线段到真实零件的映射

## L4：主材核心判定层

对真实零件做“是否属于主体核心”的判定。

规则不是“持续性前几名”，而是：

1. 该零件是否在多数站位参与主截面；
2. 去除该零件后主截面是否变化；
3. 去除后闭合关系是否消失；
4. 去除后截面家族是否变化；
5. 去除后主尺寸是否显著变化。

只有满足以上条件的零件，才能进入 `core_body_parts`。

## L5：主体家族判定层

在已经得到 `core_body_parts` 后，才允许判 H / BOX / T / CROSS / IRREGULAR。

判定依据必须来自：

- 多数稳定站位上的主截面证明
- 而不是原始板链数量投票

## L6：标准型材 / 规则拼焊反推层

在家族明确后，再判断：

- 是否匹配标准尺寸
- 是否属于规则 built-up
- 是否属于变截面

这一步不能反向影响家族定义。

## L7：解释与置信度层

每次输出必须明确：

- 为什么判成这个家族
- 依据哪些真实零件
- 哪些零件被排除为附属件
- 哪些站位参与了主要证据
- 是否需要复核

---

## 五、输出契约重构建议

后续主体识别输出必须区分三层对象：

### 1. 真实零件层

- `source_part_ids`
- `source_main_part_id`
- `source_body_seed_part_ids`

### 2. 解释层

- `stable_station_ids`
- `section_topology_by_station`
- `core_proof`
- `accessory_exclusion_reasons`

### 3. 结论层

- `body_family`
- `body_definition_satisfied`
- `body_definition_fail_reasons`
- `core_body_part_ids`
- `body_accessory_part_ids`
- `review_required`

关键要求：

- 虚拟板链只允许作为中间对象存在；
- 最终对外输出必须能直接回到真实零件编号。

---

## 六、验收标准建议

后续验收不再只看“family 是否猜对”，而是拆成三层指标：

## 6.1 主材核心识别指标

- 真实主体核心板件识别准确率
- 主体附属件剥离准确率
- 核心件去除敏感性是否符合人工判断

## 6.2 截面家族识别指标

- H / BOX / T / CROSS / IRREGULAR 家族准确率
- 未知/复核触发合理率

## 6.3 解释一致性指标

- 输出是否包含真实零件编号
- 是否能说明每个核心零件为什么进入主材
- 是否能说明每个附属件为什么被剥离

---

## 七、与当前实现的差距

当前实现已具备可复用部分：

- 零件特征抽取
- 主轴估计
- 板链合并
- 关系图构建
- 牛腿附属簇处理

当前实现明显不满足的部分：

1. 缺少真正的横截面迹线重建；
2. 主体家族判定仍是板链投票；
3. 主材核心没有通过“去除重算”验证；
4. 合成主体与真实零件语义混杂；
5. 输出对真实主体零件来源不够显式；
6. `H / BOX / T` 不是工程定义驱动。

---

## 八、建议的重构顺序

不建议一口气推倒重来，建议按以下顺序推进：

### 第 1 步：定义冻结

先冻结本文档中的：

- 主材定义
- 家族定义
- 主体核心与附属件边界定义

### 第 2 步：输出先行

在不改变主判定器前，先补齐：

- 真实零件来源输出
- 核心件/附属件解释输出
- 站位证据输出

### 第 3 步：实现横截面重建原型

先对少量典型样本实现：

- 稳定站位求交
- 迹线清洗
- 主轮廓识别

### 第 4 步：实现“去除重算”主体核心判定

这是主材识别是否可靠的关键步骤。

### 第 5 步：家族判定从“投票器”迁移到“定义判定器”

让 H / BOX / T 的结论来自定义验证，不再来自板链投票。

### 第 6 步：最后再做 profile / built-up 细分

---

## 九、对当前项目的直接建议

在新的主体基座完成前，当前项目对主材结论应采用更保守口径：

- 可以输出“主体候选”
- 可以输出“主体种子零件”
- 可以输出“当前启发式家族猜测”
- 但不应把 `H / BOX / T` 当成最终可信工程结论

特别是以下类型样本应默认进入复核：

- 核心筒钢骨柱
- 多腔 / 多壁组合柱
- 带大量内部纵向板的箱柱
- 家族投票接近的样本
- 合成主体依赖上游 `MainClass` 的样本

---

## 十、最终结论

当前主材识别的问题，本质不是“阈值没调好”，而是：

- 用启发式板链投票，替代了工程定义；
- 用家族猜测，替代了主截面证明；
- 用虚拟件近似，替代了真实主材零件解释。

因此，后续正确方向应当是：

**从“板链投票识别器”重构为“工程定义驱动的截面证明器”。**

只有这样，主体家族、主材板件和附属件剥离，才会真正稳定、可解释、可验收。
