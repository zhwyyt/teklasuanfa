# 项目整编与重新推进计划

## 文档目的

本文档用于把当前项目从“长期演化、分支混杂、样本驱动修补”的状态，收拢回“冻结基线、分层排查、单主题推进”的稳定节奏。

本文档不替代：

- [MAIN_BODY_REBUILD_STATUS.md](</I:/autoteklasuanfa/MAIN_BODY_REBUILD_STATUS.md>)
- [MAIN_BODY_REBUILD_TASKLIST.zh-CN.md](</I:/autoteklasuanfa/MAIN_BODY_REBUILD_TASKLIST.zh-CN.md>)

它的职责是给出一个更高层、更稳的执行框架，避免后续继续在旧分支、旧假设、旧语义上来回摇摆。

---

## 当前问题

项目当前的主要问题不是“完全没有方向”，而是“方向太多，边界不够清楚”：

1. 上游输入修正、下游 proof 收紧、家族映射、sidecar 观察层、Excel 导出、文档清理曾并行推进。
2. 历史阶段中存在多轮实验分支、临时验证目录、局部回退和重新尝试。
3. 一部分规则已经可以视为定版，但在口头讨论、状态板、代码、分支语义中还没有被统一冻结。
4. 一部分旧路径虽然事实上已经废弃，但仍然以字段、旁路结果、历史措辞、旧分支存在，容易再次混入主线判断。

因此，下一阶段不应继续沿“看到一个样本就补一条”的节奏推进，而应先完成项目整编。

---

## 整编目标

这轮整编只做四件事：

1. 明确哪些结论已经冻结，后续不再回摆。
2. 明确哪些旧路径已经废弃，后续不得重新混入最终判定。
3. 明确从现在开始只允许怎样的分支和推进方式。
4. 把后续问题统一拆到固定层级里排查，而不是直接对最终结果打补丁。

---

## 已冻结结论

以下内容应视为当前项目的冻结基线。

### 1. 上游真实纵向中线重建算法已定版

上游不再允许使用“候选端点投影排序后直接连线”的旧做法。

当前冻结算法为：

1. 从 longitudinal solid-edge 候选构图。
2. 对纵向边连通关系做主组件筛选。
3. 对成对平行主组件按同里程取中点。
4. 输出真实 member centerline。

这条基线已经确认是正确方向，后续不再回到“单边板边线即主轴”或“端点硬拼 guide”的旧口径。

### 2. guide 候选范围修正已定版

上游 `HasUsableGuideSegments(...)` 不再只认 `PolyBeam`。

当前冻结口径为：

- `PolyBeam + Beam`

这不是样本特判，而是 guide 候选定义修正。真实主件可能是 `Beam`，因此 `Beam` 不能再被排除在 guide 候选之外。

### 3. 采样链必须消费重建后的轴

上游一旦导出了 `GuidePolyline / AxisSegments`，后续：

- `DefaultSectionSampler`
- `DefaultAnomalyDetector`
- `ApproximateSectionIntersectionService`

都必须统一以重建后的 longitudinal axis 为准，不能再局部退回旧 `MainAxis`。

### 4. 围合证据必须基于真实边界，而不是纯计数近似

以下旧近似不再作为可信闭环依据：

- `wallCandidates >= 4`
- `2 flange + 2 web => enclosure=0.45`

当前冻结方向为：

- 边界覆盖
- 角点接触
- 横竖方向资格

### 5. 下游最终家族判定不再依赖上游 MainClass 猜类

上游 `FirstPassMemberClassifier` 这类旧式打分猜类逻辑，不能再驱动最终家族归属。

当前冻结口径为：

- 上游 `SourceMemberMainClassCode` 仅保留为 audit / sidecar / 对账字段
- 下游最终家族判定只看工程定义链：
  - `LeadClause`
  - `LeadClauseVerdict / PromotionReadiness`
  - `BodyDescriptorFamily / BodyDescriptorSectionType`
  - `ImportSynthesisKind`

---

## 已废弃口径

以下内容应视为已废弃，不允许重新混入主线。

### 1. 旧启发式主体识别链

旧 `Heuristic* / AssemblyAnalyzer / BodyRecognizer / BracketRecognizer` 口径，不再回到运行时主链。

### 2. 上游 MainClass 猜类驱动最终结论

以后不能再用“上游猜出来是 H / BOX，所以最终先归 H / BOX”的方式替代下游 proof。

### 3. 四边接触即闭环

不能把“四边接触”直接等同于真实围合或真实闭环。

### 4. 单样本特判式推进

看到单个编号异常后，直接围绕该样本补最终规则，这种推进方式停止使用。后续必须先定位问题所处层级，再决定是否改规则。

---

## 后续统一分层

从现在开始，所有异常都必须先定位到以下层级之一：

### 第 1 层：输入层

关注：

- 上游导出是否正确
- longitudinal axis 是否可信
- guide 选择是否正确
- section frame / station 是否正确

### 第 2 层：主体候选层

关注：

- 小板粗排除是否合理
- 主板候选集合是否稳定
- 哪些构件根本没有进入合理候选集

### 第 3 层：截面拓扑观察层

关注：

- 多切片看到的组织关系是什么
- 该层只做观察，不直接偷跑到最终结论

### 第 4 层：proof 条款层

关注：

- 哪条条款成立
- 哪条条款破坏
- 缺少什么证据导致不能进入稳定 verdict

### 第 5 层：家族映射层

关注：

- 只根据 proof + 工程定义做最终家族归属
- 不再混入上游猜类或临时直觉

---

## 新分支策略

后续分支只允许保留三种语义。

### 1. 稳定基线分支

用途：

- 保存当前确认正确、可回退的稳定版本

要求：

- 不在上面直接做实验
- 只接受已经验证过的收敛结果

### 2. 冻结里程碑分支

命名建议：

- `codex/freeze-*`

用途：

- 冻结关键里程碑
- 例如“上游轴修正冻结”“最终判定去除 SourceMainClass 依赖冻结”

### 3. 单主题工作分支

命名建议：

- `codex/work-*`

用途：

- 一次只推进一个主题

要求：

- 一个分支只处理一个问题族
- 不混入无关结构抽离、导出脚本、文档大扫除

---

## 近期执行顺序

下一阶段建议严格按下面顺序推进。

### 阶段 A：项目收拢

产物：

1. 本文档
2. 状态板中加入“冻结结论 / 废弃口径 / 新策略”入口
3. 任务板中改成按层排查和单主题推进

完成标准：

- 后续讨论时，不再反复确认已冻结基线

### 阶段 B：分支收拢

动作：

1. 选定一个新的唯一工作分支
2. 其余历史分支一律视为参考，不再继续并行开发
3. 需要保留的版本做冻结标签或冻结分支

完成标准：

- 后续所有新改动都落在一个明确工作分支

### 阶段 C：问题台账重建

动作：

把当前未收敛样本按层归档，例如：

1. 输入层问题
2. 候选层问题
3. 粗拓扑观察问题
4. proof 条款问题
5. 家族映射问题

完成标准：

- 不再按零散编号聊天推进
- 每个异常都有层级归属和下一步动作

### 阶段 D：单主题推进

建议优先主题：

- 变截面 `H / BOX` 的 proof 收敛

原因：

- 当前这类样本既能覆盖折线/变截面复杂性
- 又能直接检验“输入层修正是否真正传导到 proof 层”

完成标准：

- 一次只收一类，不与其它主题交叉污染

---

## 当前建议的工作纪律

从本文档生效后，建议执行以下工作纪律：

1. 不再在多个活跃分支同时推进。
2. 不再把上游猜类当最终依据。
3. 不再先看最终结果再回头找特例解释。
4. 先判断问题卡在哪一层，再决定是否动规则。
5. 规则修改必须优先追求普适性，不接受只服务于单编号样本的修补。

---

## 下一步建议

当前最合理的直接下一步不是继续改算法，而是：

1. 选定唯一工作分支。
2. 冻结当前认可的稳定版本。
3. 建一份按层分类的问题台账。
4. 只选一个主题继续推进。

建议第一主题：

- 变截面 `H / BOX` 的 proof 收敛与家族映射边界

因为这能同时检验：

- 上游轴线修正是否真正生效
- 多切片拓扑观察是否可靠
- proof 条款是否足够表达变截面主体
- 家族映射是否仍被旧语义污染
